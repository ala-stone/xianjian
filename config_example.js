/**
 * 仙剑H5自动签到脚本 - 配置示例
 * 
 * 使用说明：
 * 1. 复制此文件中的配置部分到 xianjian_signin.js
 * 2. 替换为你的真实账号信息
 * 3. 修改为你需要的配置参数
 */

// ================== 配置示例 ==================

// 服务器配置
// "formal" = 正式服
// "photon" = 光子服
const SERVER_TYPE = "formal";

// 账号配置示例（完整版，包含所有必需参数）
const ACCOUNTS = [
    // 第一个账号
    {
        username: "13800138000",           // 手机号或游戏账号
        password: "your_password_here",     // 密码
        role_id: "1410682267143104932",     // 角色ID（从游戏中获取）
        role_name: "你的角色名",           // 角色名称
        server_id: "20047",                // 服务器ID
        server_name: "服务器名称",         // 服务器名称
        platform: "android",               // 平台（android/ios）
        page_id: "8",                      // 页面ID（通常为8）
        game_id: "69",                     // 游戏ID（通常为69）
        app_id: "58"                       // 应用ID（通常为58）
    },

    // 第二个账号（可选，取消注释并填入真实信息）
    // {
    //     username: "13800138001",
    //     password: "another_password",
    //     role_id: "1410682267143104933",
    //     role_name: "另一个角色",
    //     server_id: "20048",
    //     server_name: "另一个服务器",
    //     platform: "ios",
    //     page_id: "8",
    //     game_id: "69",
    //     app_id: "58"
    // },

    // 第三个账号（可选）
    // {
    //     username: "13800138002",
    //     password: "third_password",
    //     role_id: "1410682267143104934",
    //     role_name: "第三个角色",
    //     server_id: "20049",
    //     server_name: "第三个服务器",
    //     platform: "android",
    //     page_id: "8",
    //     game_id: "69",
    //     app_id: "58"
    // }
];

// 通知配置
const NOTIFY_SUCCESS = true;   // 签到成功是否发送通知
const NOTIFY_FAIL = true;     // 签到失败是否发送通知

// ================== Cron时间配置示例 ==================

/*
圈X定时任务配置格式：分 时 日 月 周

常用时间配置示例：

每天早上8点：
0 8 * * *

每天中午12点30分：
30 12 * * *

每天晚上8点：
0 20 * * *

每天早上8点和晚上8点执行两次：
0 8,20 * * *

每30分钟执行一次：
*/30 * * * *

工作日（周一至周五）早上9点：
0 9 * * 1-5

每天早上8点、12点、18点执行：
0 8,12,18 * * *

每月1号早上8点：
0 0 8 1 * *
*/

// ================== 圈X配置示例 ==================

/*
在圈X配置文件的 [Task_local] 部分添加：

# 仙剑H5自动签到 - 每天8点执行
0 8 * * * https://你的脚本地址/xianjian_signin.js, tag=仙剑签到, enabled=true

# 如果需要多个时间段：
# 0 8,20 * * * https://你的脚本地址/xianjian_signin.js, tag=仙剑签到, enabled=true
*/

// ================== 完整的圈X配置示例 ==================

/*
复制以下内容到圈X配置文件：

[Task_local]
# 仙剑H5自动签到
0 8 * * * https://你的脚本地址/xianjian_signin.js, tag=仙剑签到, enabled=true

[MITM]
hostname = h5.qingcigame.com
*/

// ================== 快速开始步骤 ==================

/*
1. 准备工作：
   - 确保已安装 Quantumult X (圈X)
   - 准备好你的游戏账号和密码
   - 确认账号所属的服务器（正式服/光子服）

2. 配置账号：
   - 打开 xianjian_signin.js 文件
   - 找到配置区域
   - 修改 ACCOUNTS 数组，添加你的账号信息

3. 上传脚本：
   - 将脚本上传到可访问的服务器
   - 或者使用 GitHub Raw URL

4. 配置圈X：
   - 打开圈X配置文件编辑
   - 在 [Task_local] 中添加定时任务配置
   - 保存配置

5. 测试运行：
   - 在圈X任务列表中找到"仙剑签到"
   - 手动运行一次
   - 查看日志和通知确认成功

6. 完成：
   - 脚本将按照设定时间自动运行
   - 签到结果会通过通知告知
*/

// ================== 注意事项 ==================

/*
⚠️ 重要提醒：

1. 账号安全：
   - 不要将包含真实密码的脚本公开分享
   - 建议使用私有仓库
   - 定期更换密码

2. 服务器选择：
   - 确认你的账号属于哪个服务器
   - 正式服：填写 "formal"
   - 光子服：填写 "photon"

3. 签到时间：
   - 建议设置在每日早上（如 8:00）
   - 避免在游戏高峰期签到
   - 可以设置多个时间段以确保签到成功

4. 多账号：
   - 可以添加多个账号，脚本会自动依次执行
   - 每个账号之间会自动添加延迟
   - 签到结果会分别记录

5. 日志查看：
   - 在圈X"调试"->"日志"中查看详细执行情况
   - 搜索"仙剑"关键字快速定位
   - 记录会显示每个账号的签到结果
*/
