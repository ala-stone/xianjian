# 仙剑H5自动签到脚本使用说明

## 脚本简介

这是一个专为 Quantumult X (圈X) 设计的仙剑H5自动签到脚本，支持账号密码登录和自动签到功能。

## 功能特点

- ✅ 支持账号密码登录
- ✅ 自动每日签到
- ✅ 支持多账号批量签到（v1.1 新增）
- ✅ 智能检查签到状态，避免重复签到
- ✅ 详细的日志输出
- ✅ 签到结果通知

## 使用步骤

### 第一步：配置账号信息

打开 `xianjian_signin_final.js` 文件，修改账号配置区域：

```javascript
// 账号配置（支持多个账号）
const ACCOUNTS = [
    {
        username: "13569284920",           // 替换为你的账号
        password: "your_password_1",        // 替换为你的密码
        role_id: "1410682267143104932",     // 角色ID（通过抓包获取）
        role_name: "你的角色名",           // 角色名称
        server_id: "20047",                // 服务器ID（通过抓包获取）
        server_name: "Q0047 身世浮沉",      // 服务器名称
        platform: "android",               // 平台：android 或 ios
        page_id: "8",                      // 页面ID（通常为8）
        game_id: "69",                     // 游戏ID（通常为69）
        app_id: "58"                       // 应用ID（通常为58）
    },
    // 添加第二个账号
    {
        username: "13800138001",
        password: "your_password_2",
        role_id: "1410682267143104933",
        role_name: "另一个角色",
        server_id: "20048",
        server_name: "Q0048 新手区",
        platform: "ios",
        page_id: "8",
        game_id: "69",
        app_id: "58"
    }
    // 可以继续添加更多账号...
];
```

**重要提示：**
- 请将 `username` 和 `password` 替换为你的实际账号和密码
- `role_id`、`server_id` 等参数需要通过抓包获取
- 可以添加多个账号，脚本会自动依次执行签到
- 详细的参数获取方法请参考 `GET_ACCOUNT_INFO.md`
- 快速配置指南请参考 `MULTI_ACCOUNT_QUICK_START.md`

### 第二步：上传脚本到圈X

**方法一：使用本地文件**
1. 将 `xianjian_signin.js` 文件上传到你的服务器或云存储
2. 确保可以通过 HTTP/HTTPS 访问该文件
3. 获取脚本的完整 URL 地址

**方法二：使用 GitHub**
1. 将脚本上传到 GitHub 仓库
2. 使用 GitHub Raw 获取可访问的 URL
3. 例如：`https://raw.githubusercontent.com/你的用户名/仓库名/main/xianjian_signin.js`

### 第三步：配置圈X

1. **打开 Quantumult X**
2. **点击右下角的配置文件图标（小风车）**
3. **点击"配置文件编辑"**
4. **找到 `[Task_local]` 部分，添加以下配置：**

```ini
# 仙剑H5自动签到
0 8 * * * https://你的脚本地址/xianjian_signin_final.js, tag=仙剑签到, enabled=true
```

**配置说明：**
- `0 8 * * *`：表示每天早上 8:00 执行签到（可修改为任意时间）
  - 格式：`分 时 日 月 周`
  - 例如：`30 9 * * *` = 每天 9:30 执行
  - `*/30 * * * *` = 每30分钟执行一次
- `tag=仙剑签到`：在圈X任务列表中显示的名称
- `enabled=true`：启用该任务

5. **保存配置**
6. **点击"调试"->"构造请求"查看任务是否加载成功**

### 第四步：测试运行

1. 在圈X任务列表中找到"仙剑签到"任务
2. 手动点击运行一次
3. 查看日志输出和通知
4. 确认签到成功后，即可等待自动执行

## 高级配置

### 自定义签到时间

修改 cron 表达式来自定义执行时间：

| 表达式 | 说明 |
|--------|------|
| `0 8 * * *` | 每天 8:00 |
| `30 9 * * *` | 每天 9:30 |
| `0 8,12,18 * * *` | 每天 8:00, 12:00, 18:00 |
| `0 9 * * 1-5` | 工作日（周一至周五）9:00 |
| `0 0 1 * *` | 每月1号 0:00 |

### 多账号配置示例

```javascript
const ACCOUNTS = [
    {
        username: "13569284920",
        password: "password1",
        role_id: "1410682267143104932",
        role_name: "剑客小强",
        server_id: "20047",
        server_name: "Q0047 身世浮沉",
        platform: "android",
        page_id: "8",
        game_id: "69",
        app_id: "58"
    },
    {
        username: "13800138001",
        password: "password2",
        role_id: "1410682267143104933",
        role_name: "法师阿珍",
        server_id: "20048",
        server_name: "Q0048 新手区",
        platform: "ios",
        page_id: "8",
        game_id: "69",
        app_id: "58"
    },
    {
        username: "13800138002",
        password: "password3",
        role_id: "1410682267143104934",
        role_name: "射手阿飞",
        server_id: "20049",
        server_name: "Q0049 热血区",
        platform: "android",
        page_id: "8",
        game_id: "69",
        app_id: "58"
    }
];
```

**注意：**
- 每个账号需要完整的参数信息
- 不同账号可以来自不同服务器
- 如果不知道如何获取参数，请参考 `GET_ACCOUNT_INFO.md`
- 快速配置请查看 `MULTI_ACCOUNT_QUICK_START.md`

### 通知设置

```javascript
// 签到通知设置
const NOTIFY_SUCCESS = true;   // 签到成功是否通知
const NOTIFY_FAIL = true;     // 签到失败是否通知
```

## 常见问题

### Q1: 脚本无法运行？

**解决方法：**
1. 检查脚本 URL 是否可访问（建议使用 HTTPS）
2. 检查圈X是否已启用 Task 功能
3. 查看圈X日志，确认是否有错误信息
4. 确认账号密码是否正确

### Q2: 登录失败？

**可能原因：**
- 账号或密码错误
- 服务器类型选择错误（光子服/正式服）
- 账号被封禁或异常

**解决方法：**
1. 在网页版手动登录验证账号密码
2. 确认选择的 `server` 类型是否正确
3. 查看脚本日志中的错误信息

### Q3: 签到失败？

**可能原因：**
- 已经签到过（每天只能签到一次）
- 登录 token 过期
- 网络连接问题

**解决方法：**
1. 检查签到时间（建议设置在每日早上）
2. 查看日志中的详细错误信息
3. 检查网络连接

### Q4: 如何查看详细日志？

**方法：**
1. 在圈X中点击"调试"
2. 选择"日志"
3. 搜索"仙剑"关键字
4. 查看脚本输出的详细日志

## 接口说明

### 登录接口
- URL: `https://h5.qingcigame.com/api/login`
- 方法: POST
- 参数:
  ```json
  {
    "login_type": "password",
    "account": "账号",
    "password": "密码",
    "server": "formal"
  }
  ```
- 返回: 包含 token 的响应

### 签到接口
- URL: `https://h5.qingcigame.com/api/signin`
- 方法: POST
- Headers:
  - `Authorization: Bearer {token}`
- 参数:
  ```json
  {
    "server": "formal"
  }
  ```
- 返回: 签到结果和累计天数

## 安全提示

⚠️ **重要安全提醒：**

1. **账号密码安全**
   - 不要将包含真实密码的脚本上传到公开的 GitHub 仓库
   - 建议使用私有仓库或加密存储账号信息
   - 定期更换密码

2. **脚本更新**
   - 关注游戏官方公告，避免使用已被封禁的脚本
   - 定期检查脚本是否需要更新接口地址

3. **使用风险**
   - 自动签到可能违反游戏服务条款
   - 使用本脚本产生的任何后果由用户自行承担

## 脚本更新

如需更新脚本，请关注：
- 官方接口是否变更
- 登录/签到流程是否有调整
- 是否需要添加新的功能

## 技术支持

如遇到问题：
1. 查看本文档的"常见问题"部分
2. 检查圈X日志输出
3. 确认网络连接正常
4. 验证账号信息是否正确

## 更新日志

### v1.1 (2026-03-13)
- ✅ 修复多账号支持问题
- ✅ 脚本现在可以依次处理所有账号
- ✅ 优化日志输出，显示每个账号的执行进度
- ✅ 添加账号间分隔符，便于查看
- ✅ 更新配置示例文档
- ✅ 新增多账号快速配置指南

### v1.0 (2026-03-13)
- ✅ 初始版本发布
- ✅ 支持单账号自动签到
- ✅ 支持账号密码登录
- ✅ 智能检查签到状态

## 相关文档

- **多账号修复说明**: `MULTI_ACCOUNT_FIX.md`
- **多账号快速开始**: `MULTI_ACCOUNT_QUICK_START.md`
- **获取账号信息**: `GET_ACCOUNT_INFO.md`
- **错误排查指南**: `ERROR_FIX_GUIDE.md`
- **成功案例参考**: `SUCCESS_GUIDE.md`
- **接口详细说明**: `INTERFACE_GUIDE.md`
- **配置示例文件**: `config_example.js`

## 免责声明

本脚本仅供学习和个人使用，请勿用于商业用途。使用本脚本所产生的一切后果由使用者自行承担。

---

**最后更新时间：2026-03-13**
